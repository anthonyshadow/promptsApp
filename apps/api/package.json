{
  "name": "@promptopts/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/app.ts",
      "import": "./src/app.ts"
    }
  },
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "tsc -b && bun build src/index.ts --target bun --outdir dist",
    "typecheck": "tsc -b",
    "start": "bun src/index.ts"
  },
  "dependencies": {
    "@promptopts/admin-core": "workspace:*",
    "@promptopts/eval-core": "workspace:*",
    "@promptopts/eval-runner": "workspace:*",
    "@promptopts/model-registry": "workspace:*",
    "@promptopts/prompt-core": "workspace:*",
    "@promptopts/shared": "workspace:*",
    "hono": "^4.6.16",
    "zod": "^3.24.1"
  }
}
