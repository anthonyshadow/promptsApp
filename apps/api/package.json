{
  "name": "@promptopts/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "tsc -b && bun build src/index.ts --target bun --outdir dist",
    "typecheck": "tsc -b",
    "start": "bun src/index.ts"
  },
  "dependencies": {
    "@promptopts/admin-core": "workspace:*",
    "@promptopts/shared": "workspace:*",
    "hono": "^4.6.16",
    "zod": "^3.24.1"
  }
}
